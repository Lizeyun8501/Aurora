// Aurora Desktop — Tauri v2 library entry placeholder.
//
// Real Tauri command handlers are registered here in production.

pub fn run() {
    // Placeholder: in production this builds the Tauri app via
    // `tauri::Builder::default().invoke_handler(...).run()`.
}
